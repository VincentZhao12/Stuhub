import { Button, Container, Row, Col } from "react-bootstrap";
import React from 'react';
import './Viewer.scss';

const Viewer = ({userClasses, userTeaching, classData}) => {
    return (
        <div></div>
    );
}

export default Viewer;